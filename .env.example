# Copy to .env and fill in. .env is gitignored.

OPENROUTER_API_KEY=

# The live gate requires the key above. Without it, unit checks still run —
# but they check the harness, not the assistant. See README.md.

DEEPEVAL_TELEMETRY_OPT_OUT=YES
